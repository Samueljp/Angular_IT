vardata = [{ precio: 12 }, { precio: 34 }, { precio: 19 }];

data.forEach(function (elem) function(elem.precio++));

data.forEach(elem => {
    if (true) {
        var iva = 1.16
        var precioFinal = elem.precio * iva
        console.log(`
    Oferta:
    El precio final es ${precioFinal}`);
    }
     //console.log (iva)
});